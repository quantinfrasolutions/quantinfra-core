@using QuantInfra.Common.StaticData.Abstractions
@using QuantInfra.Sdk.StaticData
@using UI.Interfaces.StaticData
@using UI.SharedComponents.Base

@inject IUiAssetsRepository Repository

<EditableView TItem="Asset"
             TFilter="AssetFilter"
             EmptyText="No assets found"
             SearchFunc="@Repository.GetAssets"
             Filter="@Filter"
             RunSearchImmediately="false"
             Variant=@Variant
             HideActionsColumn=@(!EnableActions)
             AllowSelectRow=@AllowSelect
             SelectionMode="DataGridSelectionMode.Single"
             SelectAction=@ValueChanged>
    <SearchForm>
        <AssetsFilterForm @bind-Filter=@Filter />
    </SearchForm>
    @* <CreateDialogHeader> *@
    @*     New asset *@
    @* </CreateDialogHeader> *@
    @* <CreateDialogForm> *@
    @*     <CreateAssetForm /> *@
    @* </CreateDialogForm> *@
    <Columns>
        <RadzenDataGridColumn TItem="Asset" Property=@nameof(Asset.AssetId) Width="100px" Title="Id" />
        <RadzenDataGridColumn TItem="Asset" Property=@nameof(Asset.Name) Width="200px" Title="Name" />
        <RadzenDataGridColumn TItem="Asset" Property=@nameof(Asset.AssetType) Width="200px" Title="Type" />
        <RadzenDataGridColumn TItem="Asset" Property=@nameof(Asset.Description) MinWidth="200px" Title="Description" />
    </Columns>
</EditableView>

@code {
    [Parameter] public AssetFilter Filter { get; set; } = new();
    [Parameter] public bool RunSearchImmediately { get; set; }
    [Parameter] public bool AllowEdit { get; set; }
    [Parameter] public bool AllowSelect { get; set; }
    [Parameter] public Variant Variant { get; set; } = Variant.Filled;
    [Parameter] public bool EnableActions { get; set; } = true;
    [Parameter] public EventCallback<Asset> ValueChanged { get; set; }
}